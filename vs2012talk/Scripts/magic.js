

var magicNameSpace = {
    doStuff: function (x, y) {
        /// <param name="x" type="int">magic number</param>
        /// <param name="y" type="string">magic string</param>

        var a = x + 0;
        var b = 'your string was: "' + y + '"';

        alert(b + ' and your number was: ' + a);
    }
}